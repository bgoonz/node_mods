import Form from './Form';
import { Values } from './FormStorage';
declare type Handler = (values: Values, editedValues: Values) => Promise<void> | void;
export default class SubmitControl {
    private readonly form;
    private handler?;
    constructor(form: Form);
    setHandler(handler: Handler): void;
    canSubmit(): string | void;
    startSubmit(): Promise<void>;
    private runSubmitHandler;
    private afterSubmitSuccess;
}
export {};
