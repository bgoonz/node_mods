import Form from './Form';
export declare type Handler = (errors: {
    [index: string]: string;
}, values: {
    [index: string]: any;
}) => void;
export default class ValidateControl {
    private readonly form;
    private handler?;
    constructor(form: Form);
    setHandler(handler: Handler): void;
    validate(): string | void;
}
