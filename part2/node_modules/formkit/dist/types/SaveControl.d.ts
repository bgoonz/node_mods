import Form from './Form';
import { Values } from './FormStorage';
declare type Handler = (values: Values) => Promise<void> | void;
export default class SaveControl {
    private readonly form;
    private readonly debouncedSave;
    private handler?;
    constructor(form: Form);
    getSavePromise(): Promise<void>;
    cancel(): void;
    flush(): void;
    setHandler(handler: Handler): void;
    canSave(): string | void;
    startSaving(isImmediately: boolean): Promise<void>;
    private doSave;
}
export {};
